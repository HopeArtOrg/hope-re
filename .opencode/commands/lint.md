---
description: Run ESLint and fix all linting errors
agent: build
---

Run `pnpm lint` to check for ESLint errors across the codebase.
If there are errors, run `pnpm format` to auto-fix what can be fixed.
For remaining errors, fix them manually.
Show a summary of what was found and fixed.
